<!DOCTYPE html>
<html lang="th">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>ลงข่าวใหม่ | โรงพยาบาลเถิน</title>
    <link rel="icon" href="/favicon.ico" type="image/x-icon" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
   <header class="bg-white border-bottom py-3 sticky-top">
        <div class="container d-flex justify-content-between align-items-center">
            <div class="d-flex align-items-center gap-3">
                 <a href="/" class="brand-logo">
                    <img src="/favicon.ico" alt="Hospital Logo" style="width: 32px; height: 32px;">
                    <span class="brand-text">ประชาสัมพันธ์โรงพยาบาลเถิน</span>
                </a>
            </div>
            
            <div class="header-actions d-flex gap-2">
                <a href="/" class="btn btn-outline-secondary btn-sm d-flex align-items-center gap-2 rounded-pill">
                    <i class="bi bi-house-door-fill"></i> หน้าแรก
                </a>
                <a href="/admin/upload" class="btn btn-primary btn-sm d-flex align-items-center gap-2 rounded-pill shadow-sm">
                    <i class="bi bi-plus-lg"></i> ลงข่าวใหม่
                </a>
                <a href="/admin/logout" class="btn btn-danger btn-sm d-flex align-items-center gap-2 rounded-pill">
                    <i class="bi bi-box-arrow-right"></i> Logout
                </a>
            </div>
        </div>
    </header>

    <main class="container py-5">
      <div class="row justify-content-center">
        <div class="col-lg-8">
          <h2 class="fw-bold text-dark mb-4">ลงข่าวประชาสัมพันธ์ใหม่</h2>

          <% if (success) { %>
          <div
            class="alert alert-success d-flex align-items-center border-0 shadow-sm mb-4"
          >
            <i class="bi bi-check-circle-fill me-2 fs-5"></i>
            <div>บันทึกข้อมูลสำเร็จ!</div>
          </div>
          <% } %>

          <div class="card shadow-sm border-0 rounded-4">
            <div class="card-body p-4">
              <form
                id="uploadForm"
                action="/admin/upload"
                method="POST"
                enctype="multipart/form-data"
              >
                <input type="hidden" name="start_date" id="real_start_date" />
                <input type="hidden" name="end_date" id="real_end_date" />

                <div class="row g-3">
                  <div class="col-md-8">
                    <label class="form-label fw-bold">หัวข้อข่าวสาร</label>
                    <input
                      type="text"
                      name="title"
                      class="form-control"
                      placeholder="ระบุชื่อเรื่อง..."
                      required
                    />
                  </div>
                  <div class="col-md-4">
                    <label class="form-label fw-bold">หมวดหมู่</label>
                    <select name="category" id="category" class="form-select" required>
    <option value="ข่าวสารประชาสัมพันธ์">ข่าวสารประชาสัมพันธ์</option> <option value="ประชุมอบรม / สัมมนา">ประชุมอบรม / สัมมนา</option>
    <option value="ประกาศรับสมัครงาน">ประกาศรับสมัครงาน</option>
    <option value="ข่าวสารความรู้">ข่าวสารความรู้</option>
</select>
                  </div>

                  <div class="col-12"><hr class="text-muted opacity-25" /></div>

                  <div class="col-md-6">
                    <label class="form-label fw-bold text-danger"
                      ><i class="bi bi-file-earmark-pdf"></i> ไฟล์ PDF
                      (ถ้ามี)</label
                    >
                    <input
                      type="file"
                      name="pdf_file"
                      class="form-control"
                      accept="application/pdf"
                    />
                    <div class="form-text small">
                      รองรับไฟล์ .pdf ขนาดไม่เกิน 20MB
                    </div>
                  </div>

                  <div class="col-md-6">
                    <label class="form-label fw-bold text-success"
                      ><i class="bi bi-images"></i> รูปภาพประกอบ</label
                    >
                    <input
                      type="file"
                      name="images"
                      class="form-control"
                      multiple
                      accept="image/*"
                    />
                    <div class="form-text small">
                      เลือกได้หลายภาพ (JPG, PNG)
                    </div>
                  </div>

                  <div class="col-12"><hr class="text-muted opacity-25" /></div>

                  <div class="col-12">
                    <label class="form-label fw-bold text-danger"
                      ><i class="bi bi-youtube"></i> Link YouTube (ถ้ามี)</label
                    >
                    <input
                      type="text"
                      name="youtube_link"
                      class="form-control"
                      placeholder="เช่น https://www.youtube.com/watch?v=..."
                    />
                  </div>

                  <div class="col-12"><hr class="text-muted opacity-25" /></div>

                  <div class="col-12">
                    <div class="bg-light p-3 rounded-3 border">
                      <h6 class="fw-bold mb-3 text-secondary">
                        <i class="bi bi-clock"></i> กำหนดช่วงเวลาแสดงผล
                      </h6>
                      <div class="row g-3">
                        <div class="col-md-6">
                          <label class="form-label small text-muted"
                            >เริ่มแสดง (Online)</label
                          >
                          <input
                            type="text"
                            id="ui_start_date"
                            class="form-control datetime-picker"
                            placeholder="เลือกวันเวลา..."
                            required
                          />
                        </div>
                        <div class="col-md-6">
                          <label class="form-label small text-muted"
                            >สิ้นสุด (Offline)</label
                          >
                          <input
                            type="text"
                            id="ui_end_date"
                            class="form-control datetime-picker"
                            placeholder="เลือกวันเวลา..."
                            required
                          />
                        </div>
                      </div>
                    </div>
                  </div>

                  <div class="col-12 mt-4">
                    <button
                      type="submit"
                      class="btn btn-primary w-100 py-2 rounded-pill fw-bold shadow-sm"
                    >
                      <i class="bi bi-cloud-upload-fill me-2"></i> ลงประกาศ
                    </button>
                  </div>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />

    <script>
      // Config Flatpickr และเชื่อมค่าไปยัง Hidden Inputs
      const config = {
        enableTime: true,
        dateFormat: "Y-m-d H:i",
        time_24hr: true,
        minuteIncrement: 1,
        // เมื่อมีการเลือกเวลา ให้เอาค่าไปใส่ใน Hidden Input ทันที
        onChange: function (selectedDates, dateStr, instance) {
          if (instance.input.id === "ui_start_date") {
            document.getElementById("real_start_date").value = dateStr;
          } else if (instance.input.id === "ui_end_date") {
            document.getElementById("real_end_date").value = dateStr;
          }
        },
      };

      flatpickr("#ui_start_date", config);
      flatpickr("#ui_end_date", config);
    </script>
  </body>
</html>
