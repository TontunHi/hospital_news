# Hospital News Management System (ระบบจัดการข่าวประชาสัมพันธ์โรงพยาบาล)

ระบบจัดการข่าวประชาสัมพันธ์สำหรับโรงพยาบาล พัฒนาด้วย Node.js, Express และ MySQL รองรับการจัดการข่าวสาร, การอัปโหลดไฟล์ (รูปภาพ/PDF), และระบบยืนยันตัวตนแบบ 2 ขั้นตอน (2FA) ผ่าน Email

## ✨ ฟีเจอร์หลัก (Features)

### 👥 ส่วนของผู้ดูแลระบบ (Admin Panel)
- **ระบบล็อกอิน (Authentication):**
  - เข้าสู่ระบบด้วย Username/Password
  - **2FA (Two-Factor Authentication):** ยืนยันตัวตนผ่านรหัส OTP ที่ส่งไปยัง Email
- **จัดการข่าวสาร (News Management):**
  - ดูรายการข่าวสารทั้งหมด
  - **สร้างข่าวใหม่ (Create):** กำหนดหัวข้อ, หมวดหมู่, ลิงก์ YouTube, วันที่แสดงผล
  - **อัปโหลดไฟล์ (File Upload):** รองรับการแนบรูปภาพและไฟล์ PDF ได้หลายไฟล์
  - **แก้ไขข่าว (Edit):** แก้ไขข้อมูลย้อนหลัง, ลบไฟล์แนบเดิม, เพิ่มไฟล์ใหม่
  - **ลบข่าว (Delete):** ลบข่าวและไฟล์ที่เกี่ยวข้องออกจากระบบ
  - **Rich Text / SEO Friendly:** สร้าง Slug อัตโนมัติจากหัวข้อข่าว

### 🌐 ส่วนหน้าเว็บทั่วไป (Public View)
- แสดงรายการข่าวสารตามหมวดหมู่ (ประกาศจัดซื้อจัดจ้าง, ข่าวประชาสัมพันธ์, ฯลฯ)
- แสดงข่าวเฉพาะช่วงวันที่กำหนด (Start Date - End Date)
- หน้าอ่านรายละเอียดข่าว พร้อมแสดงรูปภาพและลิงก์ดาวน์โหลดไฟล์ PDF
- หน้าคลังข่าวเก่า (Archive) สำหรับข่าวที่หมดอายุแล้ว

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
- **Backend:** Node.js, Express.js
- **Database:** MySQL
- **Template Engine:** EJS
- **Authentication:** Express Session, Crypto (Hashing), Nodemailer (OTP Email)
- **File Handling:** Multer
- **Styling:** CSS / Bootstrap (Assumed based on typical EJS projects)

## ⚙️ การติดตั้งและเริ่มต้นใช้งาน (Installation)

### 1. Clone Project
```bash
git clone <repository_url>
cd hospital_news
```

### 2. ติดตั้ง Dependencies
```bash
npm install
```

### 3. ตั้งค่าฐานข้อมูล (Database Setup)
1. ตรวจสอบว่ามี MySQL Server ทำงานอยู่
2. สร้าง Database และ Table โดยใช้ไฟล์ `database.sql` ที่เตรียมไว้ให้:
   ```bash
   mysql -u root -p < database.sql
   ```
   *คำสั่งนี้จะสร้าง Database ชื่อ `hospital_news` และข้อมูล Admin เริ่มต้นให้*

### 4. ตั้งค่า Environment Variables (.env)
แก้ไขไฟล์ `.env` ให้ตรงกับการตั้งค่าเครื่องของคุณ (โดยเฉพาะ Database และ Email):

```env
# ตั้งค่า Server
PORT=3000

# ตั้งค่า Database
DB_HOST=localhost
DB_USER=root
DB_PASS=YOUR_DB_PASSWORD  # <-- แก้ไขรหัสผ่าน Database ของคุณที่นี่
DB_NAME=hospital_news

# ตั้งค่า Session
SESSION_SECRET=Your_Secure_Session_Key

# ตั้งค่า Email (สำหรับส่ง OTP)
EMAIL_SERVICE=gmail
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password # <-- ต้องใช้ App Password ของ Gmail
```

### 5. เริ่มต้น Server
สำหรับ Development:
```bash
npm start
```
หรือ
```bash
node app.js
```

เข้าใช้งานได้ที่: `http://localhost:3000`

## 🔐 ข้อมูลเข้าระบบเริ่มต้น (Default Login)
* **URL:** `/admin/login`
* **Username:** `admin`
* **Password:** `admin123`
* **Email:** `pisutyimkuson@gmail.com` (OTP จะถูกส่งไปที่นี่สำหรับการทดสอบ หรือแก้ไขในตาราง `users` เพื่อเปลี่ยนอีเมล)

## 📂 โครงสร้างโปรเจกต์ (Folder Structure)
- `controllers/` - Logic การทำงานต่างๆ (Auth, News, Public)
- `models/` - การเชื่อมต่อและ Query ฐานข้อมูล
- `routes/` - กำหนด URL Routing
- `views/` - หน้าจอแสดงผล (EJS Templates)
- `public/` - ไฟล์ Static (CSS, JS, Images)
- `uploads/` - โฟลเดอร์เก็บไฟล์ที่อัปโหลด
- `utils/` - ฟังก์ชันช่วยเหลือ (Mailer, Slug generator)
- `database.sql` - สคริปต์สร้างฐานข้อมูล
